# Python (a venv): the LinkML generators and the adapters
linkml>=1.7
jsonref>=1.1
pyyaml>=6
jsonschema>=4.18   # standard 2020-12 validator, used by tests/validate.py
# (no npm dependency: the Zod converter is in generate.py)
